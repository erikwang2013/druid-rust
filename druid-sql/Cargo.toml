[package]
name = "druid-sql"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "SQL parser and AST for Druid-Rust — lexer, parser, formatter, schema visitor"
keywords = ["druid", "sql", "parser", "ast"]
categories = ["parsing"]

[dependencies]
druid-core = { path = "../druid-core" }
tracing.workspace = true

[dev-dependencies]
